"""Core relation machinery: the fluent API, the unified visitor, node types,
protocols, and native materialization (`materialization.py`).

No public re-exports here — `mountainash.relations.core.materialization`'s
types (`NativeExecutionValue`, `MaterializationScope`, ...) are internal;
public collection and egress stay on `Relation` (`relations/__init__.py`).
"""
